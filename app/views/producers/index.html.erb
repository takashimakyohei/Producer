<% @producers.each do |producer| %>
  <div class="content">
    <%= link_to "ユーザー詳細", producer_path(producer) %>
    <p>生産者画像：<%= image_tag producer.image %></p>
    <p>生産者：<%= producer.name %></p>
  </div>
<% end %>