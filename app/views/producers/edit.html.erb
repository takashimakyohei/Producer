
<div class="form">
  <h2 class="form-title">生産者編集</h2>
    <%= form_for @producer do |f| %>

    <div class="form-wrapper">
      <p class="form-text"><%= f.label "名前" %><br />
      <div class="form-input"><%= f.text_field :name, autofocus: true, autocomplete: "name" %></div>
    </div> 

    <div class="form-wrapper">
      <p class="form-text"><%= f.label "メールアドレス" %><br />
      <div class="form-input"><%= f.email_field :email, autofocus: true, autocomplete: "email" %></div>
    </div>


    <div class="form-wrapper">
      <p class="form-text"><%= f.label "パスワード" %> </p>
      <div class="form-input"><%= f.password_field :password, autocomplete: "new-password" %></div>
    </div>

    <div class="form-wrapper">
      <p class="form-text"><%= f.label "パスワード(再確認)" %></p>
      <div class="form-input"><%= f.password_field :password_confirmation, autocomplete: "new-password" %></div>
    </div>

    <div class="form-wrapper">
      <p class="form-text"><%= f.label "出身県" %></p>
      <div class="form-input"><%= f.collection_select :prefecture_id, Prefecture.all, :id, :name, include_blank: '選択して下さい' %></div>
    </div>

    <div class="form-wrapper">
      <p class="form-text"><%= f.label "プロフィール画像" %></p>
      <%= f.file_field :image, autofocus: true, autocomplete: "image" %>
    </div>

    <div class="form-wrapper">
      <p class="form-text"><%= f.label "プロフィール" %><br />
      <div class="form-input"><%= f.text_area :profile, size: "35x5", autofocus: true, autocomplete: "profile" %></div>
    </div> 


    <div class="actions">
      <%= f.submit "更新する" %>
    </div>
  <% end %>

</div>

